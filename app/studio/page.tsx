import { TestWorkspace } from "../test/page";

export const metadata = {
  title: "制作工作台 · LUMERA",
  description: "从商品图片到动态商拍视频的五步制作流程",
};

export default function StudioPage() {
  return <TestWorkspace embedded />;
}
